// Stub for three/webgpu — not available in three@0.160; three-globe uses it
// only as an optional enhancement, so exporting empty objects is safe.
export const StorageInstancedBufferAttribute = undefined;
export const WebGPURenderer = undefined;
